services:
  - type: web
    name: skillup-django
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn skillup.wsgi:application --bind 0.0.0.0:$PORT
